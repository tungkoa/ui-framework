import { InputComponent, Key } from '../../inputComponent';
export declare class MultiSelect extends InputComponent<{
    question?: string;
    options: string[];
}> {
    private _index;
    private _selected;
    printOptions(): void;
    onKeyPress(str: string, key: Key): void;
    start(): void;
}
