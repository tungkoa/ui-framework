import { InputComponent, Key } from '../../inputComponent';
export declare class Write extends InputComponent<{
    question?: string;
    prompt?: string;
    placeholder?: string;
}> {
    private _prompt;
    private _placeholder;
    private getPrompt;
    constructor(props: any);
    onKeyPress(str: string, key: Key): Promise<void> | undefined;
    start(): void;
}
